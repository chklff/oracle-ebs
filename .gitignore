# Secrets / local config: ignore real env files, but always keep the templates
.env
*.env
!.env.example
!*.env.example

# Project-manager working copy of the spec (canonical version is docs/tech-spec.md)
/EBS-Invoice-API-Wrapper-TechSpec.md

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage/
.nyc_output/

# Python (for the future python/ implementation)
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Build / runtime
dist/
build/
*.log

# Vendored Oracle Instant Client (downloaded at deploy time, never committed)
vendor/instantclient/
vendor/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
